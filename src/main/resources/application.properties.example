spring.application.name=quiz-management-system

# ============================================
# DATABASE CONFIGURATION - FOR STUDENTS
# ============================================
# Students: Use the teacher's database server
# Teacher: Use localhost
# ============================================

# For STUDENTS: Connect to teacher's database
spring.datasource.url=jdbc:postgresql://192.168.0.103:5432/quiz_db
spring.datasource.username=postgres
spring.datasource.password=duong1235

# For TEACHER (if running on teacher's machine):
# spring.datasource.url=jdbc:postgresql://localhost:5432/quiz_db
# spring.datasource.username=postgres
# spring.datasource.password=duong1235

spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE